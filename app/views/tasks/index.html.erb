<h1>Your tasks</h1>
<div class="task">
  <%= link_to 'New Task', new_path(@tasks) %>
</div>
  <% @tasks.each do |task| %>
  <div class="container">
    <% task.completed %>
    <% if task.completed == true %>
    <%= "✔" %>
    <% else %>
    <%= "◽"  %>
    <% end %>
    <h3><%= link_to task.title, task_path(task) %></h3>

    <%= link_to '🗑', delete_path(task), method: :delete %>
  </div>
<% end %>
<br>
<br>
